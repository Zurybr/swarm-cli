---
phase: 02-skill-registry
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - src/cli/commands/skill-commands.ts
  - src/skills/index.ts
  - tests/integration/skill-cli.test.ts
autonomous: true
requirements:
  - REQ-01
user_setup: []
must_haves:
  truths:
    - CLI command 'skill register' creates a new skill with metadata
    - CLI command 'skill list' shows all registered skills
    - CLI command 'skill search <query>' finds skills by description
    - CLI command 'skill get <name>' shows detailed skill information
    - Skills module exports public API for programmatic use
  artifacts:
    - path: src/cli/commands/skill-commands.ts
      provides: CLI commands for skill management
      exports: ["registerSkillCommands"]
    - path: src/skills/index.ts
      provides: Public API exports
      exports: ["SkillRegistry", "SkillMetadata", "SkillMetadataSchema"]
    - path: tests/integration/skill-cli.test.ts
      provides: CLI integration tests
      min_tests: 4
  key_links:
    - from: skill-commands.ts
      to: SkillRegistry
      via: Command actions calling registry methods
      pattern: "registry.register|search|getAllMetadata"
    - from: CLI index.ts
      to: registerSkillCommands
      via: Program command registration
      pattern: "program.command('skill')"
---

<objective>
Create CLI commands for skill management and export the skills module public API.

Purpose: Enable users to interact with the skill registry via CLI commands.
Output: skill register, list, search, get commands with integration tests.
</objective>

<execution_context>
@/home/zurybr/.claude/get-shit-done/workflows/execute-plan.md
@/home/zurybr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-skill-registry/02-RESEARCH.md
@src/cli/index.ts

<interfaces>
From src/cli/index.ts (existing CLI pattern):
```typescript
import { Command } from 'commander';
const program = new Command();
program
  .name('swarm')
  .description('Swarm CLI')
  .version('1.0.0');
// ... other commands
program.parse();
```

From 02-02-PLAN.md (expected SkillRegistry interface):
```typescript
class SkillRegistry {
  constructor(db: sqlite3.Database)
  async initialize(): Promise<void>
  async register(metadata: SkillMetadata): Promise<void>
  getMetadata(name: string): SkillMetadata | undefined
  async search(query: string, limit?: number): Promise<SkillMetadata[]>
  getAllMetadata(): SkillMetadata[]
  async delete(name: string, version: string): Promise<void>
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create skill CLI commands</name>
  <files>src/cli/commands/skill-commands.ts</files>
  <behavior>
    - 'skill register' accepts --name, --description, --version, --category, --tags
    - 'skill list' shows all skills in formatted table
    - 'skill list --json' outputs JSON
    - 'skill list --category <cat>' filters by category
    - 'skill search <query>' searches skills by description
    - 'skill get <name>' shows detailed skill information
    - Commands handle errors gracefully with exit code 1
  </behavior>
  <action>
    Create src/cli/commands/skill-commands.ts with:
    - Import { Command } from 'commander', SkillRegistry, SkillMetadata
    - registerSkillCommands(program: Command, registry: SkillRegistry): void function
    - Create skill command group: program.command('skill').description('Manage agent skills')
    - 'register' subcommand:
      - Options: --name (required), --description (required), --version (required), --category, --tags
      - Action: build metadata, call registry.register(), log success message
      - Error handling: try/catch, console.error, process.exit(1)
    - 'list' subcommand:
      - Options: --category, --json
      - Action: getAllMetadata(), filter by category if specified
      - Format: table output with name, version, category columns or JSON if --json
    - 'search' subcommand:
      - Argument: <query> (required)
      - Option: --limit (default 10)
      - Action: registry.search(query, limit), format results with rank indicator
    - 'get' subcommand:
      - Argument: <name> (required)
      - Action: registry.getMetadata(name), if not found exit 1, else print detailed info
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="skill-cli" --passWithNoTests</automated>
  </verify>
  <done>Skill commands file exists with all four subcommands implemented</done>
</task>

<task type="auto">
  <name>Task 2: Create skills module public API</name>
  <files>src/skills/index.ts</files>
  <behavior>
    - Exports SkillRegistry class
    - Exports SkillMetadata type
    - Exports SkillMetadataSchema for validation
    - Exports validateSkillMetadata function
    - Clean public interface for consumers
  </behavior>
  <action>
    Create src/skills/index.ts with:
    - Export { SkillRegistry } from './registry/skill-registry'
    - Export { SkillMetadata, SkillSchema, Skill } from './types/skill'
    - Export { SkillMetadataSchema, validateSkillMetadata } from './schema/skill-metadata'
    - Export { SkillSearchIndex, SkillSearchResult } from './registry/search-index'
    - Export { SkillVersionManager } from './registry/version-manager'
    - Export { SkillStore } from './registry/skill-store'
    - Add JSDoc comment: "Public API for the skill registry module"
  </action>
  <verify>
    <automated>npm test -- --passWithNoTests</automated>
  </verify>
  <done>Skills module index exports all public types and classes</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire skill commands into CLI</name>
  <files>src/cli/index.ts</files>
  <behavior>
    - CLI imports registerSkillCommands
    - SkillRegistry instantiated with SQLite connection
    - Registry initialized before command execution
    - Commands have access to registry instance
  </behavior>
  <action>
    Modify src/cli/index.ts to integrate skill commands:
    - Import { registerSkillCommands } from './commands/skill-commands'
    - Import { SkillRegistry } from '../skills'
    - Import { SQLiteConnection } from '../persistence/sqlite/connection'
    - After existing command setup, add:
      - Create SQLiteConnection with default path (or from config)
      - Connect to database
      - Create SkillRegistry instance
      - Initialize registry
      - Call registerSkillCommands(program, registry)
    - Handle async initialization properly
    - Ensure database closes on process exit
  </action>
  <verify>
    <automated>npm run build</automated>
  </verify>
  <done>CLI builds successfully with skill commands integrated</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Write CLI integration tests</name>
  <files>tests/integration/skill-cli.test.ts</files>
  <behavior>
    - Can register skill via CLI command
    - Can list skills via CLI
    - Can search skills via CLI
    - Error cases handled properly
  </behavior>
  <action>
    Create tests/integration/skill-cli.test.ts with:
    - Import necessary modules, use in-memory SQLite for tests
    - Setup: create SkillRegistry, mock console.log/error for output capture
    - Test "CLI register command": call register action directly, verify registry has skill
    - Test "CLI list command": register skills, call list action, verify output contains skill names
    - Test "CLI search command": register skill with "security" in description, search "security", verify found
    - Test "CLI get command": register skill, get by name, verify details output
    - Test "CLI handles missing skill": get non-existent, verify error output
    - Use programmatic command invocation (not spawning subprocess) for speed
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="skill-cli"</automated>
  </verify>
  <done>All CLI integration tests pass (minimum 4 tests)</done>
</task>

<task type="auto">
  <name>Task 5: Run full test suite</name>
  <files></files>
  <action>
    Run the complete test suite to verify:
    - All Phase 2 tests pass
    - No regressions in Phase 1 tests
    - Build completes successfully
  </action>
  <verify>
    <automated>npm test</automated>
  </verify>
  <done>Full test suite passes (all existing + new Phase 2 tests)</done>
</task>

</tasks>

<verification>
- CLI commands work: register, list, search, get
- Skills module exports clean public API
- Integration tests verify end-to-end flow
- Full test suite passes
</verification>

<success_criteria>
- Users can register skills via CLI
- Users can list, search, and get skills via CLI
- Skills module can be imported and used programmatically
- All tests pass including integration tests
</success_criteria>

<output>
After completion, create `.planning/phases/02-skill-registry/02-03-SUMMARY.md`
</output>

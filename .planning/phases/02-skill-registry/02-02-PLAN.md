---
phase: 02-skill-registry
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/skills/registry/search-index.ts
  - src/skills/registry/version-manager.ts
  - src/skills/registry/skill-registry.ts
  - tests/unit/skills/skill-registry.test.ts
  - tests/unit/skills/skill-search.test.ts
autonomous: true
requirements:
  - REQ-01
must_haves:
  truths:
    - Skills can be discovered via full-text search on name and description
    - Search results are ranked by relevance (FTS5 rank)
    - Semantic versioning is supported for skill versions
    - Version comparison and compatibility checking works
    - SkillRegistry provides unified API for register, get, search, list
  artifacts:
    - path: src/skills/registry/search-index.ts
      provides: FTS5 search implementation
      exports: ["SkillSearchIndex"]
    - path: src/skills/registry/version-manager.ts
      provides: Semver operations
      exports: ["SkillVersionManager"]
    - path: src/skills/registry/skill-registry.ts
      provides: Main registry class
      exports: ["SkillRegistry"]
    - path: tests/unit/skills/skill-registry.test.ts
      provides: Registry unit tests
      min_tests: 8
    - path: tests/unit/skills/skill-search.test.ts
      provides: Search unit tests
      min_tests: 6
  key_links:
    - from: SkillRegistry
      to: SkillSearchIndex
      via: search() method delegation
      pattern: "this.searchIndex.search(query)"
    - from: SkillRegistry
      to: SkillVersionManager
      via: version validation and comparison
      pattern: "this.versionManager.isValid|compare|satisfies"
    - from: SkillSearchIndex
      to: skills_fts virtual table
      via: FTS5 MATCH queries
      pattern: "CREATE VIRTUAL TABLE ... USING fts5"
---

<objective>
Implement FTS5 search index, semantic version management, and the main SkillRegistry class that ties everything together.

Purpose: Enable skill discovery via search and proper version management.
Output: SearchIndex, VersionManager, SkillRegistry classes with comprehensive tests.
</objective>

<execution_context>
@/home/zurybr/.claude/get-shit-done/workflows/execute-plan.md
@/home/zurybr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-skill-registry/02-RESEARCH.md
@.planning/phases/02-skill-registry/02-01-SUMMARY.md

<interfaces>
From 02-01-PLAN.md (expected output):
```typescript
// src/skills/types/skill.ts
interface SkillMetadata {
  name: string;
  description: string;
  version: string;
  category?: string;
  tags?: string[];
  schema?: { input: any; output: any };
  author?: string;
  createdAt: Date;
  updatedAt: Date;
}

// src/skills/registry/skill-store.ts
class SkillStore {
  constructor(db: sqlite3.Database)
  async initialize(): Promise<void>
  async save(metadata: SkillMetadata): Promise<void>
  async loadAllMetadata(): Promise<SkillMetadata[]>
  async loadMetadata(name: string): Promise<SkillMetadata | undefined>
  async delete(name: string, version: string): Promise<void>
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create FTS5 search index</name>
  <files>src/skills/registry/search-index.ts</files>
  <behavior>
    - Creates virtual table skills_fts with name and description columns
    - Triggers keep FTS index in sync with skills table
    - search(query, limit) returns ranked results
    - Query sanitization prevents FTS5 syntax errors
  </behavior>
  <action>
    Create src/skills/registry/search-index.ts with:
    - Import sqlite3
    - SkillSearchResult interface with id, name, description, version, category, tags, rank
    - SkillSearchIndex class with constructor taking sqlite3.Database
    - initialize() method creating:
      - VIRTUAL TABLE skills_fts USING fts5(name, description, content='skills', content_rowid='id')
      - Trigger skills_fts_insert: AFTER INSERT ON skills, INSERT INTO skills_fts
      - Trigger skills_fts_update: AFTER UPDATE ON skills, UPDATE skills_fts
      - Trigger skills_fts_delete: AFTER DELETE ON skills, DELETE FROM skills_fts
    - search(query: string, limit: number = 10): Promise<SkillSearchResult[]> using:
      - Sanitize query: query.replace(/"/g, '""')
      - JOIN skills_fts fts JOIN skills s ON s.rowid = fts.rowid
      - WHERE skills_fts MATCH ? ORDER BY rank LIMIT ?
    - searchPrefix(prefix: string, limit: number = 5) for autocomplete using MATCH "prefix*"
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="skill-search" --passWithNoTests</automated>
  </verify>
  <done>SkillSearchIndex class exists with FTS5 virtual table and search functionality</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create version manager</name>
  <files>src/skills/registry/version-manager.ts</files>
  <behavior>
    - isValid(version) returns true for valid semver
    - compare(v1, v2) returns -1, 0, or 1
    - satisfies(version, range) checks if version matches range (e.g., "^1.0.0")
    - getLatest(versions) returns highest version
    - isCompatibleUpdate(current, candidate) checks same major version
  </behavior>
  <action>
    Create src/skills/registry/version-manager.ts with:
    - Import * as semver from 'semver'
    - SkillVersionManager class with methods:
      - isValid(version: string): boolean - semver.valid(version) !== null
      - compare(v1: string, v2: string): number - semver.compare(v1, v2)
      - satisfies(version: string, range: string): boolean - semver.satisfies(version, range)
      - getLatest(versions: string[]): string | null - semver.maxSatisfying(versions, '*')
      - isCompatibleUpdate(current: string, candidate: string): boolean - same major && gt(candidate, current)
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="version" --passWithNoTests</automated>
  </verify>
  <done>SkillVersionManager class exists with all semver operations</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create main SkillRegistry class</name>
  <files>src/skills/registry/skill-registry.ts</files>
  <behavior>
    - initialize() loads metadata index from SQLite
    - register(skill) validates, saves to store, updates index
    - get(name) returns skill with metadata and definition
    - getMetadata(name) returns just metadata
    - search(query) returns matching metadata via FTS5
    - getAllMetadata() returns all skills
    - Version validation prevents invalid versions
  </behavior>
  <action>
    Create src/skills/registry/skill-registry.ts with:
    - Imports: SkillStore, SkillSearchIndex, SkillVersionManager, SkillMetadata, validateSkillMetadata
    - SkillRegistry class:
      - private metadataIndex: Map<string, SkillMetadata>
      - private store: SkillStore
      - private searchIndex: SkillSearchIndex
      - private versionManager: SkillVersionManager
      - constructor(db: sqlite3.Database): creates store, searchIndex, versionManager
      - async initialize(): await searchIndex.initialize(), loadMetadataIndex()
      - async register(metadata: SkillMetadata): validate, check version valid, save to store, update index
      - getMetadata(name: string): SkillMetadata | undefined from index
      - async search(query: string, limit?: number): Promise<SkillMetadata[]> using searchIndex, map to metadata
      - getAllMetadata(): SkillMetadata[] from index values
      - async delete(name: string, version: string): remove from store and index
      - private loadMetadataIndex(): load from store into Map
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="skill-registry" --passWithNoTests</automated>
  </verify>
  <done>SkillRegistry class exists with full CRUD and search functionality</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Write tests for SkillRegistry</name>
  <files>tests/unit/skills/skill-registry.test.ts</files>
  <behavior>
    - Can register and retrieve skill metadata
    - Duplicate registration with same version throws error
    - Invalid version format throws error
    - Can list all registered skills
    - Can delete skills
  </behavior>
  <action>
    Create tests/unit/skills/skill-registry.test.ts with:
    - Setup: in-memory SQLite, SkillRegistry instance, initialize before tests
    - Test "should register and get skill metadata": register, getMetadata, assert match
    - Test "should reject invalid version format": try register with "invalid", assert throws
    - Test "should reject duplicate registration": register same name@version twice, assert throws on second
    - Test "should get all metadata": register 3 skills, getAllMetadata, assert length 3
    - Test "should delete skill": register, delete, assert getMetadata returns undefined
    - Test "should return undefined for non-existent skill": getMetadata("missing"), assert undefined
    - Cleanup: close database after tests
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="skill-registry"</automated>
  </verify>
  <done>All SkillRegistry tests pass (minimum 6 tests)</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Write tests for skill search</name>
  <files>tests/unit/skills/skill-search.test.ts</files>
  <behavior>
    - Search finds skills by name match
    - Search finds skills by description match
    - Search results are ranked by relevance
    - Empty query returns empty results
    - Prefix search works for autocomplete
  </behavior>
  <action>
    Create tests/unit/skills/skill-search.test.ts with:
    - Setup: SkillRegistry with search index, in-memory SQLite
    - Test "should find skill by name": register "code-review", search "code", assert found
    - Test "should find skill by description": register with description "Analyzes security", search "security", assert found
    - Test "should rank exact matches higher": register "test" and "testing", search "test", assert "test" ranked higher
    - Test "should return empty for no matches": search "xyznonexistent", assert empty array
    - Test "should limit results": register 10 skills, search with limit 5, assert length 5
    - Test "should support prefix search": register "documentation", searchPrefix "doc", assert found
  </action>
  <verify>
    <automated>npm test -- --testPathPattern="skill-search"</automated>
  </verify>
  <done>All search tests pass (minimum 6 tests)</done>
</task>

</tasks>

<verification>
- FTS5 virtual table created with proper triggers
- Search returns ranked results
- Version manager validates and compares semver correctly
- SkillRegistry integrates store, search, and version management
- All unit tests pass
</verification>

<success_criteria>
- Skills can be searched by name and description
- Search results ranked by FTS5 relevance
- Version validation and comparison works
- Registry provides unified API for all operations
</success_criteria>

<output>
After completion, create `.planning/phases/02-skill-registry/02-02-SUMMARY.md`
</output>

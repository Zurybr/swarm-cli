---
phase: 03-composable-builder
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/agents/types/composition.ts
  - src/agents/builder/schema-validator.ts
  - src/agents/builder/agent-builder.ts
  - tests/unit/agents/builder/schema-validator.test.ts
  - tests/unit/agents/builder/agent-builder.test.ts
autonomous: true
requirements:
  - REQ-02
must_haves:
  truths:
    - AgentBuilder fluent API supports chaining .use() calls
    - Schema validation detects incompatible skill input/output schemas
    - Builder validates skill existence in registry at build time
    - Validation errors provide clear messages about incompatibility
  artifacts:
    - path: src/agents/types/composition.ts
      provides: TypeScript interfaces for agent composition
      exports: [SkillConfig, CompositionConfig, CompositionValidationResult]
    - path: src/agents/builder/schema-validator.ts
      provides: JSON Schema compatibility validation
      exports: [SchemaValidator, ValidationResult]
    - path: src/agents/builder/agent-builder.ts
      provides: Fluent builder API for agent composition
      exports: [AgentBuilder]
  key_links:
    - from: AgentBuilder.build()
      to: SchemaValidator.validateChain()
      via: Internal method call
      pattern: this.schemaValidator.validateChain(resolvedSkills)
    - from: SchemaValidator
      to: SkillRegistry
      via: Skill metadata lookup
      pattern: skillRegistry.getSkill(name, version)
---

<objective>
Implement AgentBuilder fluent API and JSON Schema validation for composing skills into agents.

Purpose: Enable declarative agent composition with compile-time validation of skill compatibility.
Output: AgentBuilder class with .use() chaining, SchemaValidator for input/output compatibility, and comprehensive unit tests.
</objective>

<execution_context>
@/home/zurybr/.claude/get-shit-done/workflows/execute-plan.md
@/home/zurybr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/03-composable-builder/03-RESEARCH.md
@.planning/phases/02-skill-registry/02-03-SUMMARY.md

<interfaces>
From src/skills/types/skill.ts:
```typescript
export interface SkillSchema {
  input: Record<string, unknown>;
  output: Record<string, unknown>;
}

export interface SkillMetadata {
  name: string;
  description: string;
  version: string;
  category?: 'security' | 'performance' | 'documentation' | 'testing' | 'general';
  tags?: string[];
  schema?: SkillSchema;
  author?: string;
  createdAt: Date;
  updatedAt: Date;
}

export interface Skill {
  metadata: SkillMetadata;
  definition: Record<string, unknown>;
}
```

From 03-RESEARCH.md - Required types:
```typescript
interface SkillConfig {
  skillName: string;
  version?: string;
  config?: Record<string, unknown>;
}

interface CompositionConfig {
  name: string;
  description: string;
  skills: SkillConfig[];
  outputSkill?: string;
  globalConfig?: Record<string, unknown>;
}

interface ValidationResult {
  valid: boolean;
  errors: string[];
  warnings: string[];
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define composition types</name>
  <files>src/agents/types/composition.ts</files>
  <action>Create TypeScript interfaces for agent composition:

1. SkillConfig - skill name, optional version, optional config object
2. CompositionConfig - name, description, skills array, optional outputSkill, optional globalConfig
3. CompositionValidationResult - valid boolean, errors array, warnings array

Export all types. Use strict typing - no `any`. Document each interface with JSDoc comments explaining purpose and usage.

Reference existing patterns from src/skills/types/skill.ts for consistency.</action>
  <verify>
    <automated>npm run build -- --noEmit 2>&1 | grep -c "error TS" | grep -q "^0$"</automated>
  </verify>
  <done>Types compile without errors, exports are accessible</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement SchemaValidator with AJV</name>
  <files>src/agents/builder/schema-validator.ts, tests/unit/agents/builder/schema-validator.test.ts</files>
  <behavior>
    - Test 1: validateChain returns valid=true for compatible schemas (output has all required input fields)
    - Test 2: validateChain returns valid=false when input requires field missing from output
    - Test 3: validateChain returns valid=false for type mismatches (string vs number)
    - Test 4: validateChain returns warnings when schemas are missing (optional schemas)
    - Test 5: validateChain handles multiple skills in sequence, checking each adjacent pair
  </behavior>
  <action>Implement SchemaValidator class using AJV library:

1. Install dependencies: ajv, ajv-formats
2. Create SchemaValidator class with:
   - constructor(): Initialize AJV instance with formats
   - validateChain(skills: SkillMetadata[]): ValidationResult - Check each skill's output against next skill's input
   - Private checkCompatibility(outputSchema, inputSchema): Check if output satisfies input requirements
   - Private checkTypeCompatibility(inputType, outputType): Type coercion rules (number accepts integer, etc.)

Validation rules:
- Input required fields must exist in output properties
- Type compatibility: same type, number accepts integer
- Missing schemas generate warnings, not errors
- Accumulate all errors before returning (don't fail fast)

Use AJV for schema validation where possible. Keep custom logic minimal.</action>
  <verify>
    <automated>npm test -- --testPathPattern=schema-validator --no-coverage 2>&1 | tail -5 | grep -q "Tests:"</automated>
  </verify>
  <done>All 5 validation tests pass, AJV integration working</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement AgentBuilder fluent API</name>
  <files>src/agents/builder/agent-builder.ts, tests/unit/agents/builder/agent-builder.test.ts</files>
  <behavior>
    - Test 1: Builder chains fluently (each method returns this)
    - Test 2: build() throws if name not provided
    - Test 3: build() throws if no skills added
    - Test 4: build() throws if skill not found in registry
    - Test 5: build() throws if skill chain validation fails
    - Test 6: build() returns CompositionConfig with all properties set
    - Test 7: useVersion() pins specific skill version
    - Test 8: withGlobalConfig() merges with skill-specific configs
  </behavior>
  <action>Implement AgentBuilder class:

1. Constructor accepts SkillRegistry instance
2. Fluent methods (all return `this`):
   - withName(name: string): Set agent name
   - withDescription(desc: string): Set description
   - use(skillName: string, config?): Add skill to chain
   - useVersion(skillName, version, config?): Add skill with specific version
   - withOutput(skillName): Designate output skill
   - withGlobalConfig(config): Set global config merged into all skills

3. build(): Promise<CompositionConfig>
   - Validate name and skills present
   - Resolve all skills from registry (async)
   - Run SchemaValidator.validateChain()
   - Throw on validation errors with descriptive messages
   - Return complete CompositionConfig

4. Private resolveSkills(): Async lookup from SkillRegistry

Use SchemaValidator from Task 2. Mock SkillRegistry in tests.</action>
  <verify>
    <automated>npm test -- --testPathPattern=agent-builder --no-coverage 2>&1 | tail -5 | grep -q "Tests:"</automated>
  </verify>
  <done>All 8 builder tests pass, fluent API working correctly</done>
</task>

</tasks>

<verification>
- npm test -- --testPathPattern="schema-validator|agent-builder" passes
- npm run build compiles without errors
- AgentBuilder can chain 3+ .use() calls fluently
- SchemaValidator correctly identifies incompatible schemas
</verification>

<success_criteria>
- AgentBuilder fluent API implemented with all chain methods
- SchemaValidator validates input/output compatibility using AJV
- Validation errors are descriptive and actionable
- 13+ unit tests covering builder and validator
- Types exported for use in subsequent plans
</success_criteria>

<output>
After completion, create `.planning/phases/03-composable-builder/03-01-SUMMARY.md`
</output>

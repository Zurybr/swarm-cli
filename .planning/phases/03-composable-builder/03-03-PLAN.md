---
phase: 03-composable-builder
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - src/cli/commands/agent-commands.ts
  - src/cli/index.ts
  - tests/integration/agent-cli.test.ts
autonomous: true
requirements:
  - REQ-02
must_haves:
  truths:
    - CLI command 'agent build' creates composed agent from skill names
    - CLI validates skill compatibility and reports errors clearly
    - Built agent is registered with agent registry for orchestration
    - Integration tests verify end-to-end agent composition and execution
    - CLI follows same patterns as skill commands from Phase 2
  artifacts:
    - path: src/cli/commands/agent-commands.ts
      provides: CLI commands for agent management
      exports: [registerAgentCommands]
    - path: tests/integration/agent-cli.test.ts
      provides: Integration tests for agent CLI
      min_tests: 8
  key_links:
    - from: CLI agent build command
      to: AgentBuilder
      via: Command action handler
      pattern: new AgentBuilder(skillRegistry).use(skill).build()
    - from: AgentCommands
      to: AgentRegistry
      via: Agent registration after build
      pattern: agentRegistry.register(agent, metadata)
    - from: CLI
      to: SkillRegistry
      via: Async initialization
      pattern: await skillRegistry.initialize()
---

<objective>
Implement CLI commands for building composed agents and integration tests.

Purpose: Enable users to create composed agents via CLI and verify the complete composition workflow.
Output: 'agent build' CLI command, integration with main CLI, and comprehensive integration tests.
</objective>

<execution_context>
@/home/zurybr/.claude/get-shit-done/workflows/execute-plan.md
@/home/zurybr/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-skill-registry/02-03-SUMMARY.md
@.planning/phases/03-composable-builder/03-01-PLAN.md
@.planning/phases/03-composable-builder/03-02-PLAN.md

<interfaces>
From 02-03-SUMMARY.md - CLI patterns:
```typescript
// Async command registration pattern
export function registerSkillCommands(program: Command, skillRegistry: SkillRegistry): void

// Table output for list commands
console.table(results)

// JSON option for scripting
if (options.json) console.log(JSON.stringify(results, null, 2))
```

From 03-01-PLAN.md - AgentBuilder interface:
```typescript
class AgentBuilder {
  withName(name: string): this
  withDescription(desc: string): this
  use(skillName: string, config?): this
  withOutput(skillName: string): this
  build(): Promise<CompositionConfig>
}
```

From 03-02-PLAN.md - ComposedAgent interface:
```typescript
class ComposedAgent extends BaseAgent {
  constructor(config: AgentConfig, compositionConfig: CompositionConfig, skillRegistry: SkillRegistry)
  execute(task: Task): Promise<AgentResult>
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement agent CLI commands</name>
  <files>src/cli/commands/agent-commands.ts</files>
  <behavior>
    - Test 1: registerAgentCommands adds 'agent' subcommand to program
    - Test 2: 'agent build' with valid skills succeeds and outputs success message
    - Test 3: 'agent build' with incompatible skills fails with validation error
    - Test 4: 'agent build' with missing skill name fails with error
    - Test 5: 'agent build --json' outputs agent config as JSON
    - Test 6: 'agent build' with --config file loads skill configs from JSON
  </behavior>
  <action>
Implement agent CLI commands following Phase 2 patterns:

1. registerAgentCommands(program: Command, skillRegistry: SkillRegistry):
   - Create 'agent' command group
   - Add subcommands with descriptions

2. 'agent build' subcommand:
   - Options:
     * --name <name> (required): Agent name
     * --skills <skills> (required): Comma-separated skill names
     * --description <desc>: Agent description
     * --output <skill>: Output skill name
     * --config <file>: JSON config file with skill configurations
     * --json: Output as JSON instead of table
   - Action:
     * Parse skills: options.skills.split(',').map(s => s.trim())
     * Create AgentBuilder with skillRegistry
     * Set name, description
     * For each skill: builder.use(skillName)
     * If --output: builder.withOutput(options.output)
     * If --config: Load JSON and apply skill-specific configs
     * Call await builder.build()
     * Create ComposedAgent instance
     * Register with agentRegistry
     * Output success message (or JSON if --json)
   - Error handling:
     * Catch validation errors, output clear message
     * Exit with code 1 on failure

3. Helper functions:
   - parseSkillConfig(filePath): Load and validate JSON config
   - formatAgentOutput(agent, options): Table or JSON formatting

Follow existing CLI patterns from skill-commands.ts for consistency.
  </action>
  <verify>
    <automated>npm run build 2>&1 | grep -c "error TS" | grep -q "^0$"</automated>
  </verify>
  <done>CLI commands compile, follow established patterns</done>
</task>

<task type="auto">
  <name>Task 2: Wire agent commands into CLI</name>
  <files>src/cli/index.ts</files>
  <action>
Integrate agent commands into main CLI:

1. Import registerAgentCommands from './commands/agent-commands'
2. In the async initialization block (after skill registry setup):
   - registerAgentCommands(program, skillRegistry)
3. Ensure agent commands are registered after skill commands (dependency order)
4. Verify CLI help shows 'agent' command group

Pattern from 02-03-SUMMARY.md:
```typescript
// In async IIFE after skill registry init
registerAgentCommands(program, skillRegistry);
```
  </action>
  <verify>
    <automated>node dist/cli/index.js --help 2>&1 | grep -q "agent"</automated>
  </verify>
  <done>CLI help shows agent commands, integration complete</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Create integration tests for agent CLI</name>
  <files>tests/integration/agent-cli.test.ts</files>
  <behavior>
    - Test 1: Full flow - register skills, build agent, execute via CLI
    - Test 2: Build agent with incompatible skills fails with clear error
    - Test 3: Build agent with non-existent skill fails appropriately
    - Test 4: Agent build with --config applies skill configurations
    - Test 5: Agent build --json outputs valid JSON
    - Test 6: Built agent can be retrieved from agent registry
    - Test 7: Agent execution produces expected output format
    - Test 8: Multiple agents can be built and registered independently
  </behavior>
  <action>
Create integration tests for agent CLI:

1. Test setup:
   - Use in-memory SQLite for skill registry (from Phase 2 pattern)
   - Create test skills with compatible schemas
   - Initialize skill registry and agent registry

2. Test helpers:
   - createTestSkill(name, inputSchema, outputSchema): Register test skill
   - runAgentBuild(args): Execute CLI command and capture output
   - parseAgentOutput(output): Parse table or JSON output

3. Test cases:
   - Full composition flow: Register 2+ compatible skills, build agent, verify success
   - Incompatible skills: Register skills with mismatched schemas, verify validation error
   - Missing skill: Try to build with non-existent skill, verify error
   - Config file: Create temp JSON config, build with --config, verify applied
   - JSON output: Build with --json, parse and verify structure
   - Registry integration: Build agent, verify retrievable from agentRegistry
   - Execution: Build agent, call execute(), verify AgentResult structure
   - Isolation: Build two different agents, verify independent operation

4. Cleanup:
   - After each test: Clear registries, remove temp files

Follow integration test patterns from tests/integration/skill-cli.test.ts.
  </action>
  <verify>
    <automated>npm test -- --testPathPattern=agent-cli --no-coverage 2>&1 | tail -5 | grep -q "Tests:"</automated>
  </verify>
  <done>All 8 integration tests pass, end-to-end flow verified</done>
</task>

<task type="auto">
  <name>Task 4: Verify full test suite</name>
  <files></files>
  <action>
Run complete test suite to ensure no regressions:

1. Run all tests: npm test
2. Verify no failures in existing tests
3. Verify new tests pass
4. Check test coverage for new modules

Expected: 580+ total tests passing (558 existing + 22+ new from Phase 3)
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -10 | grep -E "Tests:.*passed" | grep -v "failed"</automated>
  </verify>
  <done>Full test suite passes, all 580+ tests green</done>
</task>

</tasks>

<verification>
- npm test passes with 580+ tests
- CLI 'agent build --help' shows all options
- Can build agent: swarm-cli agent build --name test --skills "skill1,skill2"
- Incompatible skills produce clear validation error
- Integration tests cover full composition workflow
</verification>

<success_criteria>
- 'agent build' CLI command implemented with all options
- CLI integrated into main swarm-cli command
- 8+ integration tests covering full workflow
- All tests pass (580+ total)
- Composed agents work end-to-end via CLI
</success_criteria>

<output>
After completion, create `.planning/phases/03-composable-builder/03-03-SUMMARY.md`
</output>

---
phase: 01-foundation
plan: 04
type: execute
wave: 3
depends_on:
  - 01-02
  - 01-03
files_modified:
  - src/testing/skill-test-harness.ts
  - src/testing/agent-test-harness.ts
  - tests/unit/testing/skill-harness.test.ts
  - tests/integration/agent-guardrail.test.ts
autonomous: true
requirements:
  - REQ-04
must_haves:
  truths:
    - SkillTestHarness provides fluent API for testing agent skills
    - Test harness integrates MockLLMClient with guardrails
    - Tests can verify LLM calls and guardrail results
    - Integration test shows guardrails blocking unsafe skill execution
  artifacts:
    - path: src/testing/skill-test-harness.ts
      provides: Test harness for individual skills
      exports: [SkillTestHarness]
    - path: src/testing/agent-test-harness.ts
      provides: Test harness for full agents
      exports: [AgentTestHarness]
    - path: tests/unit/testing/skill-harness.test.ts
      provides: Unit tests for test harness
      exports: []
    - path: tests/integration/agent-guardrail.test.ts
      provides: Integration test for guardrails with agents
      exports: []
  key_links:
    - from: SkillTestHarness
      to: MockLLMClient
      via: constructor injection
    - from: SkillTestHarness
      to: BaseGuardrail
      via: withGuardrail method
    - from: tests/integration/agent-guardrail.test.ts
      to: src/security/composite-guardrail.ts
      via: import and usage
---

<objective>
Create reusable test harnesses that integrate MockLLMClient with security guardrails for comprehensive agent testing.

Purpose: Provide developers with high-level testing utilities that combine deterministic LLM responses with security validation, enabling complete agent skill testing in a single fluent API.
Output: SkillTestHarness and AgentTestHarness classes with full integration tests.
</objective>

<execution_context>
@/home/zurybr/.claude/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@/home/zurybr/workspaces/swarm-cli/.planning/phases/01-foundation/01-RESEARCH.md
@/home/zurybr/workspaces/swarm-cli/src/agents/base-agent.ts

## Prior Plans Context

From 01-02:
- MockLLMClient with fixture-based responses
- FixtureLoader for pattern matching
- LLM call tracking

From 01-03:
- BaseGuardrail with priority-based execution
- PromptInjectionGuardrail and ContentSafetyGuardrail
- CompositeGuardrail for defense-in-depth

## Research Context

From 01-RESEARCH.md Pattern 3: Test Harness for Agent Skills
- Reusable test harness with mock dependencies
- Assertion helpers for LLM calls
- Guardrail integration for security testing
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create SkillTestHarness class</name>
  <files>src/testing/skill-test-harness.ts</files>
  <action>
Create src/testing/skill-test-harness.ts:

1. SkillTestHarness<TSkill, TInput, TOutput> class:
   - Constructor takes skillFactory: (llmClient: LLMClient) => TSkill
   - Private mockLLMClient: MockLLMClient
   - Private skill: TSkill
   - Private guardrails: BaseGuardrail<any, any>[] = []

2. Fluent API methods (return `this` for chaining):
   - givenLLMResponse(promptPattern: RegExp | string, response: CompletionResult): this
   - withGuardrail(guardrail: BaseGuardrail<any, any>): this
   - withFixtures(fixtures: LLMResponseFixture[]): this

3. Execution method:
   - async whenSkillExecutes(input: TInput): Promise<{ result: TOutput; llmCalls: LLMCall[]; guardrailResults: GuardrailResult<any>[] }>
   - Runs guardrails first (if any), throws GuardrailBlockedError if blocked
   - Executes skill with input
   - Returns result, tracked LLM calls, and guardrail results

4. Assertion helpers:
   - expectLLMCallContaining(expectedSubstring: string): void
   - expectLLMCallCount(expectedCount: number): void
   - expectGuardrailBlocked(guardrailName?: string): void

5. Reset method:
   - reset(): void - clears fixtures, calls, and guardrails

Reference Pattern 3 from 01-RESEARCH.md.
  </action>
  <verify>
    <automated>cat src/testing/skill-test-harness.ts | grep -q "class SkillTestHarness" && cat src/testing/skill-test-harness.ts | grep -q "whenSkillExecutes" && echo "SkillTestHarness defined"</automated>
  </verify>
  <done>SkillTestHarness with fluent API and assertion helpers is created</done>
</task>

<task type="auto">
  <name>Task 2: Create AgentTestHarness class</name>
  <files>src/testing/agent-test-harness.ts</files>
  <action>
Create src/testing/agent-test-harness.ts:

1. AgentTestHarness class for testing full BaseAgent implementations:
   - Constructor takes agentFactory: (config: AgentConfig, llmClient: LLMClient) => BaseAgent
   - Private mockLLMClient: MockLLMClient
   - Private agent: BaseAgent
   - Private guardrails: BaseGuardrail<any, any>[] = []

2. Configuration methods:
   - withAgentConfig(config: Partial<AgentConfig>): this
   - withLLMResponse(promptPattern: RegExp | string, response: CompletionResult): this
   - withGuardrail(guardrail: BaseGuardrail<any, any>): this

3. Execution method:
   - async whenAgentExecutes(task: Task): Promise<{ result: AgentResult; llmCalls: LLMCall[]; guardrailResults: GuardrailResult<any>[] }>
   - Runs guardrails on task input
   - Calls agent.execute(task)
   - Returns full result with tracking data

4. State inspection:
   - getAgentStatus(): string
   - getAgentId(): string
   - getLLMCalls(): LLMCall[]

5. Reset method for test isolation

This provides higher-level testing for complete agent workflows.
  </action>
  <verify>
    <automated>cat src/testing/agent-test-harness.ts | grep -q "class AgentTestHarness" && cat src/testing/agent-test-harness.ts | grep -q "whenAgentExecutes" && echo "AgentTestHarness defined"</automated>
  </verify>
  <done>AgentTestHarness for full agent testing is created</done>
</task>

<task type="auto">
  <name>Task 3: Update testing module index</name>
  <files>src/testing/index.ts</files>
  <action>
Update src/testing/index.ts to export new harnesses:

Add exports:
- SkillTestHarness
- AgentTestHarness
- GuardrailBlockedError (from security)

Also re-export from security for convenience:
- BaseGuardrail
- GuardrailContext
- GuardrailResult

This allows single-import usage:
import { SkillTestHarness, MockLLMClient, PromptInjectionGuardrail } from '@/testing';
  </action>
  <verify>
    <automated>cat src/testing/index.ts | grep -q "SkillTestHarness" && cat src/testing/index.ts | grep -q "AgentTestHarness" && echo "Index updated with harnesses"</automated>
  </verify>
  <done>src/testing/index.ts exports SkillTestHarness and AgentTestHarness</done>
</task>

<task type="auto" tdd="true">
  <name>Task 4: Write SkillTestHarness unit tests</name>
  <files>tests/unit/testing/skill-harness.test.ts</files>
  <behavior>
    - Test 1: SkillTestHarness executes skill with mocked LLM response
    - Test 2: givenLLMResponse supports regex pattern matching
    - Test 3: withGuardrail blocks execution when guardrail triggers
    - Test 4: expectLLMCallContaining verifies prompt content
    - Test 5: reset clears all state (fixtures, calls, guardrails)
    - Test 6: Multiple LLM calls are tracked in order
    - Test 7: Guardrail results are returned in execution result
  </behavior>
  <action>
Create tests/unit/testing/skill-harness.test.ts:

Create a simple test skill for testing the harness:
```typescript
class TestSkill {
  constructor(private llm: LLMClient) {}
  async analyze(code: string): Promise<{ issues: string[] }> {
    const result = await this.llm.complete({ prompt: `Analyze: ${code}` });
    return JSON.parse(result.content);
  }
}
```

Test SkillTestHarness:
1. Basic execution with fixture
2. Regex pattern matching in givenLLMResponse
3. Guardrail integration (use PromptInjectionGuardrail)
4. LLM call assertions
5. Reset functionality
6. Multiple calls tracking
7. Guardrail results in output

Use the harness's fluent API throughout tests.
  </action>
  <verify>
    <automated>npm test -- tests/unit/testing/skill-harness.test.ts 2>&1 | tail -10 | grep -E "PASS|FAIL"</automated>
  </verify>
  <done>All SkillTestHarness unit tests pass</done>
</task>

<task type="auto" tdd="true">
  <name>Task 5: Write integration test for guardrails with agents</name>
  <files>tests/integration/agent-guardrail.test.ts</files>
  <behavior>
    - Test 1: Agent with guardrails allows safe task execution
    - Test 2: Agent with guardrails blocks task with prompt injection
    - Test 3: CompositeGuardrail runs multiple guards in sequence
    - Test 4: Guardrail results are accessible after execution
  </behavior>
  <action>
Create tests/integration/agent-guardrail.test.ts:

Test the full integration of:
- BaseAgent (from src/agents/base-agent)
- MockLLMClient (deterministic responses)
- Security guardrails (PromptInjection + ContentSafety)
- AgentTestHarness

Create a simple concrete agent for testing:
```typescript
class TestReviewAgent extends BaseAgent {
  private llm: LLMClient;

  constructor(config: AgentConfig, llm: LLMClient) {
    super(config);
    this.llm = llm;
  }

  async execute(task: Task): Promise<AgentResult> {
    await this.beforeExecute(task);
    try {
      const result = await this.llm.complete({
        prompt: `Review: ${task.description}`
      });
      return { success: true, output: result.content };
    } catch (error) {
      return { success: false, error: String(error) };
    }
  }
}
```

Test scenarios:
1. Safe task executes successfully with guardrails
2. Task with "ignore previous instructions" is blocked
3. CompositeGuardrail with both guards
4. Verify guardrail results metadata

Use AgentTestHarness for all tests.
  </action>
  <verify>
    <automated>npm test -- tests/integration/agent-guardrail.test.ts 2>&1 | tail -10 | grep -E "PASS|FAIL"</automated>
  </verify>
  <done>Integration tests pass showing guardrails working with agents</done>
</task>

<task type="auto">
  <name>Task 6: Run full test suite and verify</name>
  <files></files>
  <action>
Run complete test suite:
- npm test

Verify:
- All mock LLM tests pass (from 01-02)
- All guardrail tests pass (from 01-03)
- All harness unit tests pass
- All integration tests pass
- No test pollution (tests pass in random order)

Run with --verbose if needed to see all test names.
  </action>
  <verify>
    <automated>npm test 2>&1 | tail -10 | grep -q "Test Suites:.*passed" && echo "Full suite passes"</automated>
  </verify>
  <done>Complete test suite passes with all unit and integration tests</done>
</task>

<task type="auto">
  <name>Task 7: Create example test demonstrating usage</name>
  <files>tests/example-skill.test.ts</files>
  <action>
Create tests/example-skill.test.ts as documentation/example:

Show a complete example of testing an agent skill with:
1. SkillTestHarness setup
2. Fixture-based LLM responses
3. Guardrail integration
4. Assertions on LLM calls
5. Reset between tests

This serves as living documentation for developers.

Example structure:
```typescript
describe('Example: Testing a Code Review Skill', () => {
  const harness = new SkillTestHarness(
    (llm) => new CodeReviewSkill(llm)
  );

  beforeEach(() => harness.reset());

  it('detects security issues', async () => {
    harness
      .givenLLMResponse(/Review this code/, securityFixture)
      .withGuardrail(new PromptInjectionGuardrail());

    const { result } = await harness.whenSkillExecutes({ code: '...' });

    expect(result.issues).toHaveLength(1);
    harness.expectLLMCallContaining('Review this code');
  });
});
```
  </action>
  <verify>
    <automated>npm test -- tests/example-skill.test.ts 2>&1 | tail -5 | grep -q "PASS" && echo "Example test passes"</automated>
  </verify>
  <done>Example test exists and passes, demonstrating best practices</done>
</task>

</tasks>

<verification>
- [ ] SkillTestHarness provides fluent API (givenLLMResponse, withGuardrail, whenSkillExecutes)
- [ ] AgentTestHarness supports full BaseAgent testing
- [ ] Both harnesses integrate MockLLMClient and guardrails
- [ ] Unit tests verify harness functionality
- [ ] Integration tests show guardrails blocking unsafe operations
- [ ] Example test demonstrates usage patterns
- [ ] Full test suite passes
</verification>

<success_criteria>
Complete testing infrastructure is ready for Phase 2 (Skill Registry). Developers can:
1. Use SkillTestHarness for testing individual skills with deterministic LLM responses
2. Use AgentTestHarness for testing full agent workflows
3. Integrate security guardrails into any test
4. Verify LLM calls and guardrail behavior
5. Write tests that are deterministic, isolated, and fast
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-04-SUMMARY.md`
</output>
